    {# <!-- Products Start -->
    <div class="container-fluid py-5">
        <div class="container">
            <div class="border-start border-5 border-primary ps-5 mb-5" style="max-width: 600px;">
                <h6 class="text-primary text-uppercase">Products</h6>
                <h1 class="display-5 text-uppercase mb-0">Products For Your Best Friends</h1>
            </div>
            <div class="owl-carousel product-carousel">
                {% for product in products %}
                <div class="pb-5">
                    <div class="product-item position-relative bg-light d-flex flex-column text-center">
                        <img class="img-fluid mb-4" src="{{ asset('img/' ~ product.photo) }}" alt="{{product.name}}">
                        <h6 class="text-uppercase">{{product.name}}</h6>
                        <h5 class="text-primary mb-0">{{product.price |number_format(2, '.', ',')}} €</h5>
                        <div class="btn-action d-flex justify-content-center">
                            <a class="btn btn-primary py-2 px-3 open-cart-product" data-id="{{product.id}}"><i class="bi bi-cart"></i></a>
                            <a class="btn btn-primary py-2 px-3 open-info-product" data-id="{{product.id}}"><i class="bi bi-eye"></i></a>
                        </div>
                    </div>
                </div>
                {% endfor %}
            </div>
        </div>
    </div>

    <!-- Products End --> #}

        <!-- brand -->
    <div class="brand">
        <div class="container">

        </div>
        <div class="brand-bg">
            <div class="container">
                <div class="row">
                    {% for product in products %}
                        <div class="col-xl-4 col-lg-4 col-md-4 col-sm-6 margin">
                            <div class="brand_box">
                                <img src="{{ asset('images/' ~ product.photo) }}" alt="{{product.name}}" />
                                <h3>{{product.price |number_format(2, '.', ',')}} €</h3>
                                <span>Mobile Phone</span>
                                <i><img src="images/star.png"/></i>
                                <i><img src="images/star.png"/></i>
                                <i><img src="images/star.png"/></i>
                                <i><img src="images/star.png"/></i>
                                <br>
                                <a class="btn btn-primary py-2 px-3 open-cart-product icon-products" data-id="{{product.id}}"><i class="bi bi-cart"></i></a>
                                <a class="btn btn-primary py-2 px-3 open-info-product icon-products" data-id="{{product.id}}"><i class="bi bi-eye"></i></a>
                            </div>
                        </div>
                    {% endfor %}
                    <div class="col-md-12">
                        <a class="read-more">See More</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- end brand -->